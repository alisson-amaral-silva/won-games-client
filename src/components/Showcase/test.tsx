// import { screen, render } from '@testing-library/react'

// import Showcase from '.'

describe('<Showcase  />', () => {
  it('should render the heading', () => {
    // render(<Showcase title="Most Popular" />)
    // expect(
    //   screen.getByRole('heading', { name: /Most Popular /i })
    // ).toBeInTheDocument()
  })
})
